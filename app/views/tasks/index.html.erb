<h1>タスク管理アプリ</h1>
<h2>今日のタスク一覧</h2>
<ul>
    <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %><%= task.status %> &gt; <%= task.content %></li>
    <% end %>
</ul>

<%= link_to 'タスクを新規追加する' , new_task_path %>
